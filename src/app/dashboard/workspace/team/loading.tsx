import { TeamPageSkeleton } from "@/components/workspace";

/** Header, KPI strip, then the toolbar and members table inside one card. */
export default function TeamMembersLoading() {
  return <TeamPageSkeleton />;
}
