/**
 * Moved into the ui kit — the marketing modules use it as much as commerce
 * does. Re-exported here so the existing commerce imports keep working.
 */
export { FilterBar, FilterSelect } from "@/components/ui/filter-bar";
