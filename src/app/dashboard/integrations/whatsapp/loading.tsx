import { IntegrationDetailSkeleton } from "@/components/integrations";

/** Summary card, KPI row, then the settings column and the health rail. */
export default function WhatsAppIntegrationLoading() {
  return <IntegrationDetailSkeleton />;
}
