import { ActivityPageSkeleton } from "@/components/workspace";

/** Header, KPI strip, then the filter row and audit table. */
export default function WorkspaceActivityLoading() {
  return <ActivityPageSkeleton />;
}
