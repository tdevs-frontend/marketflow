import { SocialIntegrationSkeleton } from "@/components/integrations";

/** KPI row, then the connected-account grid beside the health rail. */
export default function SocialIntegrationLoading() {
  return <SocialIntegrationSkeleton />;
}
