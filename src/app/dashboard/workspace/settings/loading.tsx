import { WorkspaceSettingsSkeleton } from "@/components/workspace";

/** Header, the section tab strip, then one section's fields. */
export default function WorkspaceSettingsLoading() {
  return <WorkspaceSettingsSkeleton />;
}
